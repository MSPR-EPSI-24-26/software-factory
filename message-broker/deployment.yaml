apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-broker
  namespace: payetonkawa-prod
  labels:
    app: message-broker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: message-broker
  template:
    metadata:
      labels:
        app: message-broker
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: message-broker
          image: ghcr.io/mspr-epsi-24-26/message-broker:00cb101e63d69d596805842ed835ad5c282f32ff
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3003"
            - name: RABBITMQ_URL
              value: "amqp://admin:admin123@rabbitmq-service:5672"
          