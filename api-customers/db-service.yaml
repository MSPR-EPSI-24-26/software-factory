apiVersion: v1
kind: Service
metadata:
  name: db-service-customers
  namespace: payetonkawa
spec:
  selector:
    app: db-customers
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP