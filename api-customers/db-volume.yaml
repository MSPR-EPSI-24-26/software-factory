apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: api-customers
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard