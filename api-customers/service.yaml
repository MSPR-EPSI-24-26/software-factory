apiVersion: v1
kind: Service
metadata:
  name: api-customers-service
  namespace: payetonkawa
spec:
  type: ClusterIP
  selector:
    app: api-customers
  ports:
    - port: 3000
      targetPort: 3000