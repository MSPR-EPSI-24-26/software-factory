apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-app
  namespace: payetonkawa-prod
  labels:
    app: frontend-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-app
  template:
    metadata:
      labels:
        app: frontend-app
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: frontend-app
          image: ghcr.io/mspr-epsi-24-26/front:dc366b2ebdfb32b0f052f8ef06ac563696eaa3cf
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3000"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi