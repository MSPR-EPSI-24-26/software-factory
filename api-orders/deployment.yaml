apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-orders
  namespace: payetonkawa
  labels:
    app: api-orders
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-orders
  template:
    metadata:
      labels:
        app: api-orders
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: api-orders
          image: ghcr.io/mspr-epsi-24-26/api-orders:98dc66e64cdc0e66a82a665287e933af857a16c7
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3001"
            - name: DATABASE_HOST
              value: "db-service-orders"
            - name: DATABASE_PORT
              value: "5432"
            - name: DATABASE_USER
              value: "user"
            - name: DATABASE_PASSWORD
              value: "password"
            - name: DATABASE_NAME
              value: "api_orders"