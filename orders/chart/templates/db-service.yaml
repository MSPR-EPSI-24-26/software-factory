apiVersion: v1
kind: Service
metadata:
  name: db-service-orders
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: db-orders
  ports:
    - port: {{ .Values.db.port }}
      targetPort: {{ .Values.db.port }}
  type: ClusterIP