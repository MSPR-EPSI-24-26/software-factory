apiVersion: v1
kind: Service
metadata:
  name: api-orders-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: api-orders
  ports:
    - port: {{ .Values.appPort }}
      targetPort: {{ .Values.targetPort }}